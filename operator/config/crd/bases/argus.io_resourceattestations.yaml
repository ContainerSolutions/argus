---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: resourceattestations.argus.io
spec:
  group: argus.io
  names:
    kind: ResourceAttestation
    listKind: ResourceAttestationList
    plural: resourceattestations
    singular: resourceattestation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.result.result
      name: Result
      type: string
    - jsonPath: .status.result.runAt
      name: Last Run
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceAttestation is the Schema for the resourceattestations
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ResourceAttestationSpec defines the desired state of ResourceAttestation
            properties:
              providerRef:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - providerRef
            type: object
          status:
            description: ResourceAttestationStatus defines the observed state of ResourceAttestation
            properties:
              result:
                properties:
                  err:
                    type: string
                  logs:
                    type: string
                  reason:
                    type: string
                  result:
                    type: string
                  runAt:
                    format: date-time
                    type: string
                required:
                - logs
                - reason
                - result
                - runAt
                type: object
              status:
                type: string
            required:
            - result
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
