---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: resourceimplementations.argus.io
spec:
  group: argus.io
  names:
    kind: ResourceImplementation
    listKind: ResourceImplementationList
    plural: resourceimplementations
    singular: resourceimplementation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceImplementation is the Schema for the resourceimplementations
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ResourceImplementationSpec defines the desired state of ResourceImplementation
            properties:
              class:
                type: string
              requirementRef:
                properties:
                  code:
                    type: string
                  version:
                    type: string
                required:
                - code
                - version
                type: object
            required:
            - class
            - requirementRef
            type: object
          status:
            description: ResourceImplementationStatus defines the observed state of
              ResourceImplementation
            properties:
              passedAttestations:
                default: 0
                type: integer
              resourceAttestations:
                items:
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              totalAttestations:
                default: 0
                type: integer
            required:
            - passedAttestations
            - totalAttestations
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
