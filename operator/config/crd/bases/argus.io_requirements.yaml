---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: requirements.argus.io
spec:
  group: argus.io
  names:
    kind: Requirement
    listKind: RequirementList
    plural: requirements
    singular: requirement
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Requirement is the Schema for the requirements API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RequirementSpec defines the desired state of Requirement
            properties:
              applicableResourceClasses:
                description: TODO define classes objects instead of a free string?
                  Strong typing means better validation
                items:
                  type: string
                type: array
              definition:
                properties:
                  category:
                    type: string
                  class:
                    type: string
                  code:
                    type: string
                  version:
                    type: string
                required:
                - category
                - class
                - code
                - version
                type: object
              requiredImplementationClasses:
                items:
                  type: string
                type: array
            required:
            - applicableResourceClasses
            - definition
            - requiredImplementationClasses
            type: object
          status:
            description: RequirementStatus defines the observed state of Requirement
            properties:
              children:
                items:
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              requirementHash:
                type: string
            required:
            - requirementHash
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
