# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/argus.io_attestations.yaml
- bases/argus.io_implementations.yaml
- bases/argus.io_requirements.yaml
- bases/argus.io_resources.yaml
- bases/argus.io_resourcerequirements.yaml
- bases/argus.io_resourceattestations.yaml
- bases/argus.io_resourceimplementations.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_attestations.yaml
#- patches/webhook_in_implementations.yaml
#- patches/webhook_in_requirements.yaml
#- patches/webhook_in_resources.yaml
#- patches/webhook_in_resourcerequirements.yaml
#- patches/webhook_in_resourceattestations.yaml
#- patches/webhook_in_resourceimplementations.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_attestations.yaml
#- patches/cainjection_in_implementations.yaml
#- patches/cainjection_in_requirements.yaml
#- patches/cainjection_in_resources.yaml
#- patches/cainjection_in_resourcerequirements.yaml
#- patches/cainjection_in_resourceattestations.yaml
#- patches/cainjection_in_resourceimplementations.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
